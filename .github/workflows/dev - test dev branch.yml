name: test dev branch
on:
  push:
    branches:
      - disabled

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: paso 1
        run: echo ${{ github.action }}

  job2:
    runs-on: ubuntu-latest
    steps:
      - name: paso 1
        run: echo ${{ github.action }}
